import { LargeInstrumentPageMain } from "@/components/large-instrument-page-main";

export const metadata = {
  title: "大型樂器預約說明｜D Festival × 幻樂空間",
};

export default function LargeInstrumentPage() {
  return <LargeInstrumentPageMain />;
}
