import { OpenSpaceBookingInfoPageMain } from "@/components/open-space-booking-info-page-main";

export const metadata = {
  title: "預約幻樂空間——開放空間（Open Space）時段｜D Festival × 幻樂空間",
};

export default function OpenSpaceBookingInfoPage() {
  return <OpenSpaceBookingInfoPageMain />;
}
